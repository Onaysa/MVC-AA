{{#each blogs as |blog| }}
<div class="row mb-4 blog">
  <div class="col-md-5">
    <h2>

      {{{get_emoji}}}
      <a href="/blog/{{comment.id}}">{{comment.name}}</a>
    </h2>
    <p>
      <span for="img" aria-label="click">💲</span>
      <span class="comment-sign">{{comment.created}}</span>
      needed!
    </p>
    <p>Created by {{blog.user.name}} on {{comment.date_created}}</p>
  </div>
  <div class="col-md-7">
    <p>
      {{comment.description}}
    </p>
  </div>
</div>
{{/each}}